# ALX Training - Zero Day Project

This project includes:

* Setting up a `Github` Repo
* **Adding**, **Commiting** and **Pushing** to `GitHub` using `Git`
* `Branching` and resloving `conflicts`
* **Pulling** `changes` and staying `up-to-date`
